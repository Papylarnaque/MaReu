<resources>

    <!-- Base application theme. -->
    <style name="MaRéu" parent="Theme.AppCompat.DayNight">
    <!--<style name="AppTheme" parent="Theme.AppCompat.DayNight">-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MaRéu.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MaRéu.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="MaRéu.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="counterOverride">
        <item name="android:textColor">#ff0000</item>
        <item name="android:textAlignment">textStart</item>
    </style>

</resources>
